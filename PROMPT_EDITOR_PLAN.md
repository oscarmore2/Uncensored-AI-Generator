# 提示词编辑器 —— 真 WYSIWYG + 选区级 AI · 工程规格

> 状态：**P0 真机已通过**（第十节）—— 真 WYSIWYG 成立，不回退。
> 结构编辑（标题/列表）已从 P4 提前落地。5 项待确认（第九节）仍未定。
> 关系：取代 `DRAFTS_AND_TEMPLATES_PLAN.md` 子系统 A 中的 Markdown 部分，其余不变。

---

## 〇、一个前提的更正

设计初期曾以「提交给模型的必须与编辑器逐字节一致」为约束
（`mini-markdown.ts` 顶部注释即按此写），据此反对真 WYSIWYG。

**这条约束在当前代码里已经不成立。** `model-ref-syntax.ts`：

> 改写只发生在**提交上游前的最后一刻**——这样换模型重跑同一条提示词才不用改文案

`@Image1` 存库是规范形式，提交时 `renderPromptRefs` 把它改写成目标模型的写法，
部分模型直接删除。也就是说**提交前序列化这个模式已经在生产里跑着**，
而且是刻意设计的、理由充分。

真 WYSIWYG 不是开新口子，是在同一模式上再加一层。这是本方案成立的基础。

---

## 一、三层文本模型（核心契约）

| 层 | 形态 | 权威性 | 存在于 | 谁能看见 |
| --- | --- | --- | --- | --- |
| **doc** | 文档树（节点 + 标记） | 编辑态权威 | 内存，可选存草稿 | 用户（渲染后） |
| **canonical** | 纯文本，`@Image1` 规范引用 | **持久化与对外的唯一权威** | Draft / Template / Generation / 分享 | 用户（原文视图） |
| **target** | 上游实收字符串 | 上游权威 | 仅提交那一刻 | **任何人都不可见** |

三条铁律：

1. **`serialize(doc) → canonical` 是纯函数。** 同一个 doc 永远得到同一个字符串。
2. **凡是离开编辑器的文本一律是 canonical。** 存库、草稿、模板、分享、AI 请求，无例外。
3. **target 层不下发。** 引用写法会暴露底层模型家族，与
   `prompt-targets.ts` / `magic-prompt.ts` 既有的「模型信息绝不下发到生成端」冲突。
   原文视图只显示 canonical。

原文视图是**必需品**，不是可选功能——真 WYSIWYG 之后它是用户唯一能看到真实
提交内容的地方。形态：按需全屏、可反向编辑（改完重新 parse）。
双栏预览由此下线，常态屏幕全部给编辑区。

---

## 二、文档 schema（封闭）

真 WYSIWYG 的全部风险来自「doc 能表达 canonical 装不下的东西」。schema 越小越安全。

### 块级

| 节点 | 编辑器呈现 | 序列化 | 出现于 |
| --- | --- | --- | --- |
| 段落 | 正常 | 原文 + `\n\n` | 全模式 |
| 分节标题 | 大一号加粗 | 纯文本行 + `\n`，**符号丢弃** | 仅视频类 |
| 无序列表 | 圆点 | `- ` 前缀 | 仅视频类 |
| 有序列表 | 数字 | `1. ` 前缀 | 仅视频类 |
| 注释块 | 灰 + 左竖线 | **整段剔除** | 全模式 |
| 分隔线 | 横线 | `\n\n` | 全模式（纯视觉） |

标题**不输出 `##`**。视频模式下 `开场镜头\n人物从左侧走入…` 本身就是镜头脚本，
`##` 对模型是纯噪声。

### 行内

| 标记 | 编辑器呈现 | 序列化 |
| --- | --- | --- |
| 素材引用 chip | 橙色胶囊 + 缩略图（atom node） | `@Image1` |
| 模板槽位 | 虚线胶囊（atom node） | 见第五节 |
| 注释 | 灰色删除线 | 剔除 |
| 字面量 | 等宽底色 | 原文（保护 `a*b` 不被误处理） |

### 不提供加粗 / 斜体

上游没有一个认识 `**`。加粗只有三种结局：污染提示词、静默失效、欺骗用户。
腾出的位置给注释标记和 chip。

用户「想强调」的需求是真的，出口在第六节的 AI 动作「加重这一句」——
用语言手段实现，按钮不撒谎。

### 模式化

schema 全模式一套，**工具栏按 `formatId` 隐藏按钮**。`image_t2i` 下不出现
标题与列表（规则原话「避免长段落叙事」），只留注释与 chip。

> **已实现**：`PromptEditor` 的 `structure` 开关。关掉时工具条和 markdown
> 快捷输入**一起**停用——只藏按钮不停快捷输入的话，敲 `- ` 照样变列表，
> 等于没关。接入 make 页时按 `formatId` 传这个 prop 即可。

---

## 三、序列化与清洗

### 空白规则（必须定死）

round-trip 不稳定的头号来源。提交路径上目前只有 `prompt.trim()`，中间空白无人管。

- 段落之间恒为 `\n\n`，列表项之间恒为 `\n`
- 空段落不产出任何字符
- 每行 `trimEnd()`
- 全文首尾 `trim()`

### 反向解析

原文视图可编辑，改完 `parse(text) → doc`。若 `parse(serialize(doc)) ≠ doc`，
**以原文为准**，doc 重建。语法保持极小就是为了让这个 round-trip 可证明。

> **实现修正（P0）**：`parse(serialize(doc)) === doc` 对含标题 / 注释 / 槽位的
> doc **不可能成立**——标题的符号已丢弃、注释已剔除、槽位已填成死文本，
> canonical 里没有任何痕迹能把它们认回来。这是 schema 设计的直接后果。
> 真正成立、且已被单测钉死的是两条：
>
> 1. `serialize(parse(t))` 幂等（40 万条随机输入验过）
> 2. `serialize(parse(serialize(d))) === serialize(d)`
>
> 代价要写进 UI：**原文视图落地会吃掉所有注释**，那个入口必须先提示。

### 粘贴清洗（白名单，非黑名单）

- 富文本一律降到本 schema，schema 外的一切丢弃
- 纯文本按 `- ` / `1. ` 识别列表
- **不可见字符必须清除**：零宽字符、U+00A0、全角空格。这些进了提示词肉眼查不出，
  模型看到的和用户以为的不是一回事

---

## 四、素材引用 chip

### 现存缺陷（本方案要治的第一件事）

`make/page.tsx:653` 换模式/换档位时 `pruneMediaToSpecs` 丢掉新模型不认的媒体，
**提示词里的 `@Image3` 无人处理**。叠加 `PromptMentionBox` 的按位置延迟绑定：
删掉第 2 张图后，`@Image3` 静默改指第 2 张。提示词一字未改，含义全变，
出片不对且无从排查。

textarea 时代无解——纯文本里认不出哪些字符是引用。chip 化之后可治。

### chip 三态

| 状态 | 呈现 | 交互 |
| --- | --- | --- |
| 正常 | 橙色胶囊 + 缩略图 | 点击 → 右侧素材栏对应项高亮并滚动到（双向定位） |
| 失效（素材已删） | 红色划掉 | 点击 → 跳到上传位 |
| 漂移（编号所指已变） | 黄色闪烁 | hover 说明「现在指向另一张图」，提供一键重绑 |

chip 内存 token 字符串 `Image1`，**不存索引**。拖动缩略图换顺序时语义不变
（仍是「第 1 张图」），只有胶囊上的缩略图跟着换——与 `PromptMentionBox`
刻意保留的延迟绑定行为一致。

### 两条实现期发现的硬约束（P0）

**一、相邻的两颗胶囊必须垫空格。** 识别式要求引用前面是行首或分隔符，
于是 `@Image3@Image1` 只有第一个算引用，第二个原样上行变成模型读不懂的字面
文字——编辑器里明明是两颗好端端的胶囊。所以序列化时遇到紧邻引用**自动垫一个
空格**，这是不变式不是体验优化（插入时也垫，那才是体验）。
`PromptMentionBox.insertToken` 早就在做同样的事，此处是同一条规矩的复用。

**二、漂移态只对「本次会话里插入的」胶囊有效。** 判定「所指已变」需要知道
插入那一刻它指向谁，所以 chip 上挂了一个 `hint`（**仅供告警，绝不参与序列化**，
token 仍是唯一权威）。而从 canonical 文本解析出来的胶囊没有 hint——
草稿恢复、套模板、作品复用进来的全都是这一类，它们只报得出红色孤儿，
报不出黄色漂移。

> 这直接影响第九节待确认 #3：**想要跨会话的漂移告警，草稿就必须附带 doc JSON**，
> 只存 canonical 文本做不到。这是「doc JSON 可选」之外新增的一条实打实的理由。

### 附带收益

- chip 是原子节点，不会被退格半删成 `@Imag`（textarea 现状会）
- `caretOffset` 那套镜像 div 量光标的 hack（约 60 行）可以删掉，
  编辑器原生提供坐标
- `renderPromptRefs` 的正则管线**完全不动**——serialize 产出的就是 `@Image1`

---

## 五、模板槽位

模板现状是整段替换死文本。文档模型让它可以带**占位槽** atom node：

```
一位 ⟦主体⟧ 站在 ⟦场景⟧ 中，⟦光线⟧，镜头缓慢推近
```

- 套用后 Tab 在槽位间跳，填完即走
- 未填槽位在提交前拦一次
- 序列化时槽位输出其当前填充值；未填则输出空并触发拦截

textarea 时代做不了：槽位会被误删、光标定位得数字符。

`TemplatePanel` 的存取链路无需改动，加的是 schema 里一个节点类型。
**这把模板从「一段死文本」变成「一个表单」，是量级差异。**

模板自带 media 快照，其 prompt 里的 `@ImageN` 与快照对齐；套用后若媒体缺失，
按第四节的失效态处理。

---

## 六、选区级 AI

### 动作清单

| 动作 | 复用 | 要点 |
| --- | --- | --- |
| 按模式润色 | `PROMPT_FORMAT_RULES[formatId]` | system prompt 加「只改写给定片段，不越界输出」 |
| 译成英 / 中 | — | **是本地化不是翻译**：「电影感光影」应出 `cinematic lighting` 而非直译。方向按**选区内容**判断，与界面语言无关 |
| 补充细节 / 延展镜头 | `formatId` 分叉 | 图像＝加材质光影不加新主体；视频＝接下一动作 + 运镜。**默认落法是「插入下方」**，必须流式 |
| 精简到上限 | 见第九节 | 上限来源待确认 |
| 加重这一句 | — | 「加粗」需求的诚实出口：换更强措辞 / 提到句首 / 适度重复 |

negative 字段虽不用 WYSIWYG（本就是逗号短语列表），但**「译成英文」与
「精简到上限」应对它同样生效**——同一条管线，只换字段。

### 共用管线

请求体：`{ action, selection, context_before, context_after, mode, tier, spicy, ... }`

- **必须带上下文**。润色一句话时模型需要知道整体在写什么，前后各截约 300 字。
- **chip 占位保护**：序列化选区时把 chip 换成 `⟦1⟧`，返回后还原。
  **模型根本没机会改写 `@Image1`**——比在 system prompt 里求它「请保留」
  可靠一个数量级。返回后校验占位符集合无增减，对不上判失败重试。
- **审查替换后的全文**，不审片段。片段脱离上下文两个方向都会误判：
  整段里越线的话孤立看无害，孤立看越线的话在整段里可能正常。
  沿用现有 `reviewPrompt` / `isBlocked` / `hasAdultAccess`。
- **同一时刻只允许一个动作在跑**，否则多张结果卡片满天飞。进度显示在浮条上。
- **锚点失效**：等结果期间用户改了别处会导致选区偏移。用编辑器的 position mapping
  跟随，或 pending 期间把该段锁住。二选一，不能不管。
- **失败就地报错**：审查拒绝、限流要在浮条原地展开，并说清是哪一句被拦。
  现在统一 toast 飞到屏幕角落，选区级操作下不可接受。

### 结果落地

浮出预览卡，四个按钮 **替换 / 插入下方 / 重试 / 放弃**，决定前原文一字不动，
整个替换是**一步撤销**。

这是对 `make/page.tsx:756` 现有 `setPrompt(data.prompt)` 直接整段覆盖的修正——
用户写十分钟，点一下按钮全没了，且无对比无撤销。

---

## 七、编辑器之外的联动

| # | 事项 | 说明 |
| --- | --- | --- |
| 1 | **四个入口都要过 `parse()`** | URL `?prompt=`（`page.tsx:137`）、草稿恢复、模板套用、作品复用。漏一个就 doc/text 不同步。模板与复用带进来的是他人文本，按不可信清洗 |
| 2 | **切模式提示** | 文生图的逗号短语切到文生视频后格式全错，现无人提醒。若提示词非空且格式明显不符，给内联提示「按新模式改写？」——已有 AI 能力的免费复用 |
| 3 | **分享剔除注释** | 注释块含私人试错记录。对外一律走 serialize 结果（本就剔除注释）。写进契约，别等出事 |
| 4 | **计数器按 token 不按字符** | 中英不等价，各模式上限不同。底部常显 `已用/上限`，超限变红并直接给「精简到上限」——**这是让该 AI 动作被发现的唯一入口** |
| 5 | **提交前体检** | 按 `formatId` 逐条比对 `PROMPT_FORMAT_RULES`，列成可折叠清单。**不拦截，只陈述**。规则现成，近乎零成本 |
| 6 | **版本快照** | AI 动作抬高「改坏了想回去」的频率，撤销栈跨不了会话。每次 AI 动作前 + 每次生成前打点，留最近 10 条，**存 localStorage，不进 Draft 表** |
| 7 | **常用片段库** | `QUALITY_TAIL` 硬编码在 `magic-prompt.ts` 说明尾巴是真需求。`/` 命令里给「我的片段」，选中任意段可「存为片段」 |

---

## 八、交互形态

| 平台 | 形态 | 理由 |
| --- | --- | --- |
| 桌面 | 选中浮动工具条 + `/` 斜杠命令 | Notion / Medium 那套 |
| 移动 | **跟随软键盘的常驻底部工具条** | iOS Safari 选中文字必弹系统菜单（拷贝/查询/翻译），浮条会被盖住或抢位。安卓稍好但也会撞 |

移动端底栏放 AI 动作 + 结构按钮 + 素材入口，选中与否只影响按钮可用态。
与现有 `PromptComposer` 横竖屏分治思路一致。

**基础编辑（复制/粘贴/剪切/删除）不自己做**，交还原生：

- 触屏没有右键，长按弹的是系统菜单，iOS 上抢不过来
- 自建「粘贴」要走 `navigator.clipboard.readText()`，各浏览器要么弹权限、
  要么限制手势、要么不给——大概率做出一个点了没反应的按钮
- 覆盖原生右键会连带干掉拼写检查、查词、系统翻译

`/` 菜单可大量复用 `PromptMentionBox`：定位、键盘导航、mousedown 抢焦点的坑
已经踩完一遍。

---

## 九、待确认

| # | 事项 | 现状 / 建议 |
| --- | --- | --- |
| 1 | **计费与限流** | 现 20 次/分，对选区级 AI 是数量级不够。建议分档：翻译/精简宽松，润色/补充细节严格；或改小额积分。**纯产品定价决定，只有你能定** |
| 2 | **长度上限来源** | `ModelCapability` 无此字段；`model-schema.ts` 只取 `default/enum/minimum/maximum`，**`maxLength` 被丢弃**。若上游 schema 有声明，加一行解析即可。**需先确认上游是否声明** |
| 3 | **草稿存储格式** | 建议**以 canonical 文本为准，doc JSON 可选附带**。文本是必须的，doc JSON 只是编辑态便利；schema 演进后老草稿靠 parse 仍能打开。反之（只存 JSON）每次改 schema 都变成数据迁移。按 Railway 约束，加可空列是安全的加法 |
| 4 | **流式** | `/api/prompts/magic` 现为一次性返回。「补充细节」不流式体验废掉一半。建议四个动作统一走 SSE，一次做完 |
| 5 | **编辑器库** | 倾向 **Lexical**：移动端与 IME 是其设计重点，core ~30KB，本产品移动端权重高。Tiptap 上手快文档全但包大三四倍，且序列化本就要自己写（目标格式不是 markdown）。**等 spike 结果** |

---

## 十、go/no-go：IME spike

真 WYSIWYG 必然 contenteditable，**安卓中文输入是唯一可能推翻整个方案的风险**。

写一个约 200 行的 spike，真机测（安卓 Chrome + 搜狗/百度输入法）：

- 中文连续输入、候选词上屏
- 退格跨 chip 边界
- 在 chip 紧邻处起输
- 长文本（2000 字）+ 实时装饰下的输入延迟（低端机）

**回退方案要说实话**：CodeMirror 6 同样基于 contenteditable，
「退回行内实时渲染」并不能规避 IME 风险。真正的回退是**留在 textarea + 双栏**。
所以 spike 直接拿 Lexical 做，一举两得——过了就是地基，不过就整体回退。

在写任何正式代码之前做这一步。

### spike 已就位

页面：**`/lab/prompt-editor`**（管理员登录后可见；服务端 `requireRole("admin")`，
不放在 `/admin` 下是因为那套布局是 w-56 固定侧栏、没做响应式，手机上正文只剩
一百多像素，测不出任何东西）。

已在桌面 Chrome 验过、不必再测的部分：

- 胶囊是真原子节点（`contenteditable=false` + Lexical decorator 包裹）
- 三态齐活：正常 / 黄色漂移 / 红色孤儿划掉，都实测触发过
- 「拔掉第 1 张图」按钮重演 `pruneMediaToSpecs` 那个 bug，孤儿即时报出来
- canonical 面板实时显示真正会离开编辑器的字符串
- 相邻胶囊自动垫空格（就是靠这一步发现的问题）
- 2000 字灌入、12+ 段、20 颗胶囊

**必须真机才能定的**，也就是页面上那四项：中文连打候选上屏、退格跨胶囊、
紧贴胶囊起输、长文本下的输入延迟。前三项在无头浏览器里测不了——
harness 的按键事件根本不产生 `keydown` / `beforeinput`（已确认，非代码问题）。

页面自带延迟采样（`beforeinput` → 下一帧，p50 / p95 / max）。
**输入事件数和量到帧数分开显示**：页面切后台时 `requestAnimationFrame` 不跑，
采样会是 0，那时页面会直接说「有输入却一帧都没量到，回到前台重测」，
而不是甩一个会被误读成「零延迟」的 0。

测完把页面最下面那段报告发回来。

### 结论：通过

真机验收通过，Lexical 扛住了安卓中文输入法。真 WYSIWYG 成立，
**不回退 textarea + 双栏**，P1 起全部解锁。

### 随后补上的结构编辑

真机通过后立刻发现编辑器只有段落和胶囊，**打不出标题和列表**——
这是计划书把结构编辑排在 P4 的后果，但它正是最初那条诉求
（「起码要跟大语言模型的输入框差不多」）。于是提前做掉：

- markdown 快捷输入：`# ` 出标题，`- ` / `* ` 出无序列表，`1. ` 出有序列表
- 常显工具条三个按钮，手机上这才是主路径——中文输入法下要打 `#`
  得来回切两次输入法，没人会用
- **transformer 走白名单**，默认那套里的加粗/斜体/行内代码/引用块一个不要，
  理由同第二节：上游没有模型认识 `**`
- 标题只有一级。`#`、`##`、`###` 都出同一级，纯粹照顾肌肉记忆——
  做成三种视觉大小等于画出一套一保存就蒸发的层级，和拒绝加粗是同一个理由

验过：`#` 与 `-` 序列化后**符号确实被丢弃**（canonical 是 `开场镜头` 和
`- 推镜`），工具条四种切换及「再点一下取消」全部正确，
从未聚焦时直接点工具条也有合理落点。

---

## 十一、其它工程事项

- ~~**无测试框架**~~ **已引入 vitest**（P0）。`npm test` 跑
  `src/lib/prompt-doc.test.ts`：65 条，含黄金样本、空白规则、清洗、
  与提交期改写的一致性交叉校验，以及上面那两条不变式。
  随机输入测试默认 3000 轮，`FUZZ=400000 npm test` 可加压——
  首次跑就逮到一个真 bug（缩进的列表项导致 normalize 不幂等）。
- **SSR**。Next 15 + contenteditable 必须 `dynamic(…, { ssr:false })`，
  并给一个与最终样式一致的 skeleton，否则首屏跳一下。
- **i18n**。`en.json` / `zh-CN.json` 两份：工具栏、`/` 命令、AI 动作名、错误文案。
- **无障碍**。contenteditable 的 a11y 弱于 textarea。浮条必须能键盘唤起，
  不能只有鼠标选中才出；工具条按钮全部 `aria-label`。

---

## 十二、分期

| 期 | 内容 | 依赖 |
| --- | --- | --- |
| **P0** | IME spike（Lexical，真机） | ✅ **真机已通过**。地基坐实 |
| **P1** | 文档模型地基 + chip 化 + 三态告警 + 四入口 parse | P0 |
| **P2** | 模板槽位 | P1 |
| **P3** | 选区级 AI（五动作 + 预览卡 + 流式） | P1；待确认 1/4 |
| **P4** | ~~结构编辑（标题/列表）~~ ✅ 已随 P0 落地；剩注释 + 原文视图 + 分享剔除 | P1 |
| **P5** | 计数器 / 体检 / 快照 / 片段库 / 双向定位 / 切模式提示 | P1；计数器待确认 2 |

按「用户体感 ÷ 工作量」排，P1 与 P2 排在结构编辑之前是有意的：

**它们都不需要 markdown，只需要文档模型。** 孤儿引用治的是静默出错，
模板槽位把已有功能提升一个量级——这两条才是本次改造的地基，
markdown 结构编辑反而是上层建筑。
