# Used when Railway Root Directory = web
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
watchPatterns = ["**"]

[deploy]
startCommand = "sh -c 'npx prisma db push && npm start'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
