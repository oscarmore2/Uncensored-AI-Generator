# Monorepo: Next.js app lives in web/
# Railway reads this from repo root when Root Directory is NOT set.
[build]
builder = "DOCKERFILE"
dockerfilePath = "web/Dockerfile"
watchPatterns = ["web/**"]

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
